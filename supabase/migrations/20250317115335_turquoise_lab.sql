/*
  # Empty migration - functionality moved
  
  The storage bucket and policy setup has been moved to a separate migration
  to avoid conflicts with existing configuration.
*/

-- No changes needed - bucket and policies already exist